name: Temporary Pythia 2.8B Actions status

on:
  push:
    branches: [main]
    paths:
      - gpu/trigger-pythia-2.8b-actions-status-v1

permissions:
  actions: read
  contents: write

jobs:
  status:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Read calibration workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python - <<'PY'
          import json, os, urllib.request
          from pathlib import Path
          repo = os.environ['GITHUB_REPOSITORY']
          req = urllib.request.Request(
              f'https://api.github.com/repos/{repo}/actions/runs?per_page=100',
              headers={
                  'Authorization': f"Bearer {os.environ['GH_TOKEN']}",
                  'Accept': 'application/vnd.github+json',
                  'X-GitHub-Api-Version': '2022-11-28',
              },
          )
          with urllib.request.urlopen(req, timeout=30) as response:
              payload = json.load(response)
          rows = []
          for run in payload.get('workflow_runs', []):
              if run.get('name') != 'RunPod Pythia 2.8B calibration':
                  continue
              rows.append({
                  'id': run.get('id'),
                  'status': run.get('status'),
                  'conclusion': run.get('conclusion'),
                  'event': run.get('event'),
                  'head_sha': run.get('head_sha'),
                  'created_at': run.get('created_at'),
                  'updated_at': run.get('updated_at'),
                  'run_number': run.get('run_number'),
                  'run_attempt': run.get('run_attempt'),
                  'html_url': run.get('html_url'),
              })
          Path('gpu/pythia-2.8b-actions-status.json').write_text(json.dumps({'runs': rows[:5]}, indent=2, sort_keys=True) + '\n')
          PY
      - name: Persist sanitized Actions status
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add gpu/pythia-2.8b-actions-status.json
          git commit -m 'Record Pythia 2.8B Actions status' || exit 0
          git push origin HEAD:main
