Evaluation-only rerun using preserved canonical checkpoints from artifact 9114652333. Recovery ZIP extraction uses Python zipfile rather than a Pod-level unzip dependency. The self-destruct watchdog is fully detached before the result-stream descriptor is opened, and the descriptor is explicitly closed immediately after the verified handoff archive is streamed through the same SSH session. All required OpenUnlearning JSON results are then revalidated locally against manifest SHA256 and byte counts before upload as a dedicated GitHub artifact; metadata is uploaded separately. Hardware restricted to clean RTX A6000/A40 under $0.70/hour. No reconstruction, training passes, or optimizer updates. Trigger 2026-08-12 preservation-rerun-v12-final.
